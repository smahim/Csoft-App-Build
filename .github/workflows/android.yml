name: Build Android APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install Cordova
        run: npm install -g cordova

      - name: Build Android
        run: |
          # ১. জিপ ফাইল আনজিপ করা
          unzip My_Csoft_App.zip
          
          # ২. কর্ডোভা প্রোজেক্ট তৈরি করা
          cordova create project com.csoft.app Csoft
          cd project
          
          # ৩. জিপের ভেতর থাকা ফোল্ডার থেকে ফাইল কপি করা
          rm -rf www/*
          cp -r ../My_Csoft_App/* www/
          
          # ৪. অ্যান্ড্রয়েড বিল্ড করা
          cordova platform add android
          cordova build android --no-telemetry

      - name: Upload APK
        uses: actions/upload-artifact@v3
        with:
          name: Csoft-APK
          path: project/platforms/android/app/build/outputs/apk/debug/app-debug.apk
